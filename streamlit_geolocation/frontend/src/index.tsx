import React from "react"
import ReactDOM from "react-dom"
import Streamlit_geolocation from "./streamlit_geolocation"

ReactDOM.render(
  <React.StrictMode>
    <Streamlit_geolocation />
  </React.StrictMode>,
  document.getElementById("root")
)